
services:
  dailypilot-bot:
    build: .
    container_name: dailypilot-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - db_data:/app/db    # БД
    networks:
      - dailypilot_net

volumes:
  db_data:

networks:
  dailypilot_net:
    driver: bridge
