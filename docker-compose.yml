
services:
  dailypilot-bot:
    build: .
    container_name: dailypilot-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/data
    networks:
      - dailypilot_net

networks:
  dailypilot_net:
    driver: bridge
